```{include} ../../../docker/README.md
```

## In the docs

**Docs notes.** The `docker run` examples above mount the current directory
as `/workspace` — run them from the directory that contains your configs,
data, and checkpoints.

Next steps:

- {doc}`../reference/cli` — the flags used in the container commands above
- {doc}`../getting-started/installation` — install KonfAI directly on the host instead
